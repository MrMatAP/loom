"""MCP tool adapter for the Catalog component -- an alternative interface
onto the same Service layer the REST API's routers call (see
docs/superpowers/specs/2026-08-08-catalog-api-design.md), so an Agent can
invoke Catalog use-cases directly as MCP tool calls instead of HTTP."""
